services:
  - type: web
    name: mozibang-activation-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend_python/sqlite_activation_api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
      - key: PORT
        generateValue: true